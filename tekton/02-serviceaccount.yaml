# The pipeline needs a ServiceAccount that can push to the internal OpenShift
# Container Registry (OCR). The 'system:image-builder' role grants push
# permission; 'builder' SA on OpenShift already has this by default, so it's
# simplest to reuse it. This file shows the explicit version for clarity.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: pipeline-sa
